//
// Created by Иван Назаров on 27.03.2023.
//

#include "functions.h"
#include <iostream>

bool is_intersect_r_r(Shape * a, Shape * b) {
    std::cout << "function for checking the intersection of two rectangles" << std::endl;
    return true;
}

bool is_intersect_r_t(Shape * a, Shape * b) {
    std::cout << "function for checking the intersection of a rectangle and a triangle" << std::endl;
    return true;
}

bool is_intersect_t_t(Shape * a, Shape * b) {
    std::cout << "function for checking the intersection of two triangles" << std::endl;
    return true;
}